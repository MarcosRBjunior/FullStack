services:
  frontend:
    build: .
    command: npm start
    ports:
      - "3000:3000"
    environment:
      HOST: 0.0.0.0

  backend:
    build: .
    command: npm run server
    ports:
      - "8000:8000"